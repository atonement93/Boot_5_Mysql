<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.iu.b5.board.qna.QnaMpaaer">
	<insert id="setInsert" parameterType="BoardVO" useGeneratedKeys="true" keyProperty="num">
		insert into qna (num, title, writer, contents, hit, regDate)
		values(#{num}, #{title}, #{contents}, #{writer}, 0, now());
	</insert>
	
	<update id="setUpdate" parameterType="BoardVO">
		update qna set title=#{title}, contents=#{contents}
		where num=#{num};
	</update>
	
	<delete id="setDelete" parameterType="BoardVO">
		delete from qna where num=#{num};
	</delete>
	
	<select id="getSelectOne" parameterType="BoardVO" resultType="QnaVO">
		select * from qna where num=#{num};
	</select>
	
	<select id="getSelectList" resultType="BoardVO">
		select * from qna order by num desc;
	</select>
</mapper>
